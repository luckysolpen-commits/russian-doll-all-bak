CHTPM+OS MULTI-PROJECT RESTORATION REPORT
Date: 2026-03-10
Status: SUCCESSFUL

1. COMPLETED FIXES:
------------------
A. INPUT CONFLICT RESOLUTION (The "Zombie Module" Fix):
   - Problem: Background modules (loader, editor, player) were all reading from the same history file simultaneously, causing "input theft" and layout jumps.
   - Fix: Implemented a "Layout Heartbeat" system. The chtpm_parser now exports 'pieces/display/current_layout.txt'.
   - Impact: Modules now check if their layout is active before responding to keys. "Zombie" background processes now remain idle.

B. PDL COMMAND ARCHITECTURE:
   - Problem: Project selection was reliant on brittle key-injection loops.
   - Fix: Updated chtpm_parser.c to support direct commands in PDL METHOD values. 
   - Impact: Buttons in loader.pdl now call 'LOAD_PROJECT:name' directly, enabling atomic state/layout transitions.

C. LOADER INDEXING & STATE:
   - Problem: Off-by-one error (key '2' loaded index 0) and incorrect active_target_id.
   - Fix: Adjusted indexing in loader_module.c and forced 'active_target_id=loader' on startup.

D. PROJECT RESTORATION:
   - demo_1: Restored the terrain map and piece positions (Hero, NPC, Chest) to match original specs.
   - template: Created a functional blank project with a "TEMPLATE PROJECT" title screen.

2. NEXT STEPS (Phase 2):
-----------------------
A. INTERACTION VALIDATION:
   - Test the 'interact' Op in demo_1 to ensure it correctly triggers the .asm scripts (npc_greeting.asm, chest_open.asm) for the specific project context.

B. SELECTOR REFINEMENT:
   - Verify that toggling focus (Key: 9) between 'hero' and 'selector' works within the playrm_module without losing project state.

C. DYNAMIC APP LOADING:
   - Now that project loading is stable, implement the "Launch FuzzPet" integration from the main menu, ensuring it doesn't conflict with the player_app state.

D. NUCLEAR CLEANUP V4:
   - Update run_chtpm.sh to reflect the new directory structure and ensure it doesn't accidentally wipe the newly restored 'template' and 'demo_1' PDL files.

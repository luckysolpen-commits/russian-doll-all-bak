PROBLEM ANALYSIS:
1. Loader Index Mismatch: loader_module.c has an off-by-one error where it uses (key - '2') for project_index. The menu labels projects starting from 1 (e.g. 1. [op-ed], 2. [demo_1]). So pressing '2' (which is demo_1) leads to index 0 (which is op-ed). This explains why demo_1 shows op-ed content.
2. Missing/Empty Projects: projects/template is empty and needs a default title screen map.
3. Demo 1 Map Inconsistency: The current demo_1 map doesn't match the desired visual layout (missing some icons in the terrain and piece positions).

PROPOSED SOLUTION:
1. Fix loader_module.c:
   - Update loop to handle '1' and '2' etc correctly.
   - Use (key - '1') to map to project index 0.
2. Fix projects/template:
   - Create maps/map_0001_z0.txt with "TEMPLATE PROJECT" label.
3. Fix projects/demo_1:
   - Update maps/map_0001_z0.txt to match user's requested layout.
   - Update piece states for hero, npc_01, etc. in run_chtpm.sh or directly in project folder.
4. Verify by rebuilding modules and checking state.

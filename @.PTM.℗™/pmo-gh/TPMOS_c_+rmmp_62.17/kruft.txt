# KRUFT INVESTIGATION REPORT - 2026-04-05

## 1. Redundant Log Files (Root Directory)
The following files in the project root are "kruft" from previous versions or specific project runs:
- `manager_debug.txt`: Contains logs from `p2p-net` project showing an older project path (`TPMOS_c_+rmmp_55.25`). Strings like "main: starting up" and "POLL: last_pos" are present.
- `manager_out.txt`: Zero-byte file, likely a leftover from an old redirection.
- `manager_stderr.txt`: Zero-byte file.
- `manager_stdout.txt`: Zero-byte file.
- `$null`: Zero-byte file, likely created by a shell script error or Windows-to-Linux porting glitch.

**Verdict:** Safe to remove.

## 2. Redundant Project Mirrors (pieces/apps/ vs projects/)
The user noted that projects used to be in `pieces/apps/` and have been moved to `projects/`.

- `pieces/apps/fuzz-op/` vs `projects/fuzz-op/`
- `pieces/apps/op-ed/` vs `projects/op-ed/`
- `pieces/apps/user/` vs `projects/user/`
- `pieces/apps/man-*/` vs `projects/man-*/`

**Analysis:**
- `fuzz-op`, `op-ed`, and `user` project mirrors in `pieces/apps/` were redundant and have been removed. Their state is now managed entirely in `projects/`.
- `man-add`, `man-ops`, and `man-pal` projects still have their PRIMARY layouts and modules in `pieces/apps/man-*/`.
- `pieces/os/project_routes.kvp` still routes these projects to `pieces/apps/man-*/layouts/`.
- The `projects/man-*/` directories only contain maps and piece state, not the application logic (modules).

**Verdict:** 
- `fuzz-op`, `op-ed`, and `user` mirrors: Removed (Step 3).
- `man-*` directories: **MUST KEEP** for now. Removing them would break the application logic for those projects as they haven't been fully migrated to the `projects/` structure yet.

---
**Completed Actions:**
1. Deleted root log files: `manager_debug.txt`, `manager_out.txt`, `manager_stderr.txt`, `manager_stdout.txt`, `$null`.
2. Updated `run_chtpm.sh` to remove redundant initialization of `pieces/apps/` projects.
3. Removed redundant project mirrors in `pieces/apps/` (fuzz-op, op-ed, user).

**Remaining Work:**
- None for this kruft cleanup task.

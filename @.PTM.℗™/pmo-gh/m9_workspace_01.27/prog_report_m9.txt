# prog_report_m9.txt - Progress Report 2026-03-09 (Updated)

## 1. Accomplishments
*   **Audit Documentation:** Created `demo_game_ops_audit.md` explaining the Muscles (Ops), Brains (Module), World (Projects), and Theater (Layouts).
*   **Standardized Ops Library (Muscles):**
    *   `move_player.c`: Standardized for project-specific piece state and collision detection.
    *   `render_map.c`: Implemented hierarchical rendering (Player > NPC > Selector > Terrain).
    *   `interact.c`: Standardized script execution via Prisc VM with project-relative paths.
    *   `project_loader.c`: Standardized for engine state updates (`project_id`, `current_z`).
    *   `menu_op.c`: Provided a robust stderr-based menu renderer for Prisc scripts.
*   **Player Bridge Module (Brain):**
    *   Standardized key routing for WASD (movement), XZ (floors), ENTER (interact), and 9 (focus toggle).
    *   Implemented `${last_key}` debug output for real-time input verification.
*   **Theater Handover (Eyes):**
    *   Updated `chtpm_parser.c` to be context-aware; it now dynamically selects the correct `view.txt` and `last_response.txt` based on whether the layout is in `player_app` or `fuzzpet_app`.
*   **System Optimization:**
    *   Fixed high CPU usage in `gl_renderer.c` by implementing a "dirty bit" redraw strategy (only redraw on pulse).
    *   Hardened `kill_all.sh` and `run_chtpm.sh` for reliable process cleanup and state reset.
*   **Project Isolation:**
    *   Fully instantiated `demo_1` with clean state mirrors for Hero, Selector, NPC (Old Man), and Chest.
    *   Created `DEMO-0` dummy project for proof-of-concept project selection.
*   **Project Loader:** Enabled manual project selection on startup by defaulting to `template` project.

## 2. Current Status
*   **Human-Testable State:** The system successfully launches, enters the project selection screen, and allows loading `demo_1` or `DEMO-0`.
*   **Input Verification:** Raw key codes are now visible in the UI via the `KEY:` field.
*   **Movement:** The "Hero" and "Selector" can be moved across the map using standardized Ops.

## 3. Next Steps
*   **Interaction Verification:** Confirm that pressing ENTER on the NPC or Chest triggers the corresponding `.asm` scripts correctly.
*   **Editor Phase 3:** Begin porting the "Interact Mode" and palette logic to the Editor using these same standardized Ops.
*   **Refinement:** Clean up redundant "trait" code in `fuzzpet_app` that has been superseded by the Shared Ops library.

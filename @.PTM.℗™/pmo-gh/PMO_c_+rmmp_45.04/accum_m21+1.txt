# Digit Accumulation Logic - Analysis & Re-implementation Plan

## Current State (2026-03-21)

**The digit accumulation logic is NOT present in the current codebase.**

```

**Behavior:** Each digit is processed independently and immediately.
- Press '1' → loads projects[0] (project #1)
- Press '3' → loads projects[2] (project #3)
- No accumulation occurs

---

## Desired Behavior (Digit Accumulation) - CORRECTED

**Concept:** Accumulate consecutive digits ONLY while the accumulated value *could still be valid*.
No timeout needed - menu navigates away on valid selection anyway.

### Example Flow (assuming 14 indexes):
> parser count availiable indexes 
```
User types: '1'
  → Buffer: 1 (could become 10, 11, 12, 13, 14...)
  → JUMP to 1 -but buffer wait for next digit before resetting

User types: '1' then '3'
  → Buffer: 1 → 13 (valid! ≤ 14)
  → ACTION: 
  → Menu navigates away, buffer resets naturally

User types: '3'
  → Buffer: 3 (30+ would be INVALID, > 14)
  → ACTION: jump 2 projects[2] (3-indexed) immediately

User types: '1' then '3' then '4'
  → Buffer: 1 → 13 (valid) → 134 (INVALID, > 14)
  → ACTION: just load projects[13] 
```

### The Rule:
```
```

### Edge Cases:
1. **Overflow:** `13` + `4` = `134` > 14 → just use `4`
2. **Single digit valid:** `3` → `30` > 14 → just use `3` immediately
3. **Non-digit key:** Clears buffer, no action
4. **Menu nav away:**  call resets state naturally

---

## Re-implementation Difficulty: **LOW** (simpler than timeout approach!)

### Changes Required:

#### 1. chtpm - Simple Accumulator
Need to add:
- Static accumulator: `static int accum = 0;`

## Complexity Assessment

| Aspect              | Difficulty | Notes                                      |
|---------------------|------------|--------------------------------------------|
| Code changes        | LOW        | ~20 lines added to loader_module.c         |
| State management    | LOW        | Single static int accumulator              |
| Timing logic        | NONE       | No timeout needed!                         |
| Edge case handling  | LOW        | Simple bounds checking                     |
| Testing             | LOW        | Just verify digit sequences                |
| **Overall**         | **LOW**    | Very straightforward                       |

**Estimated Implementation Time:** 15-20 minutes including testing

---

## Files to Modify
1. `pieces/apps/playrm/loader/chtpm_parser.c
---

Generated: 2026-03-21
Context: Loader frame navigation digit accumulation analysis

 this is where we left       
   off. specifically , we were moving to step 6 : 1. **Real Data Sync:**     
   Update `gl_desktop.c` to read `projects/fuzz-op-gl/session/state.txt`     
   instead of using mock grid/entities.  
   
   
   if we do a pure fuzz-op / investigation of pipeline
for updating map variables or w/e dynamically synchronized etc
so that even u understand it. <on mac , using gemini> 
then u should beable to do same thing for broken gltpm 
and guide it manually . no biggie tbh 

   ------------------------
                                       
   2. **Menu Triggers:** Map menu options (`scan`, `collect`) to actual      
   Op execution via the project manager. ; i want u to read over the         
   current exisiting code progress as it was described, find it in the       
   code and lets write an extensive indexed walk thru of it , so i can       
   know where the updates will need to fit in. lets do that in               
   glos-update-prep.txt 
   
   <literally the only thing different (betwen gltpm and chtpml and fuzzop pipeline)  should be the renderer. (terminal ascii vs gl ) (however the code structure
   maybe different if we have to include chtpm in the desktop_gl code. 
   , however we may rather compile "gl related code together a linked "
   (we usually dont do this, but we can make an exeption for gl) 


   we still dont have a "READABLE FUZOP PIPELINE FOR MAP (never asked 4 it asked 4 menu;)🤯️
   
   
      < just get it to make it based on ur understanding of the current setup ; 
   if desktop_gl is running everything (cuz gl..., then wut? 
   <then u wont beable 2 "split up" but w/e lets just dig deeper. 
   🙅‍♀️️
      i think we could even do op ed. with a clean "gltpm_parser.c"
   that works just like chtpml parser, (and runs seperate of the fuzzop one
   and lets see which we get first)
   <then u can switch back in forth in same codebase, instead of rolling back> 
   <honestly dont even ask it to reference the old one.  🙅‍♀️️



i have this version of a risc like assembler 


i want to expand it to use custom instructions (for a game engine im making)

(and custom variables, strings etc ) 

from a 

default_op.txt  with kvp pairs (of name , type , and c-binary or var value = .+x extension) 
also other ops can be included and referenced using #include alt_op.txt for example

exe 
player_id int 1234
player_name char "duck"
player_position array 1,2,3
move_player void +x/move_player.+x  {changes player_xyz <x,y,z> value}

console_print void +x/console_print.+x  {prints input to console}



2.
we should move "out" to default_op.txt because it is a custom op
seperate from the offical  riscv std...

______________________


<long term this will all be chained to a master clock
including the risc computer .it shouldn't run unless clock updates. 
(but rmmv has this same thing. some "actions activate clock" 
(some would be this way if u programed turn base behavior, etc) 
(and im sure there is a time activated mechanism as well)

---------------------
───────────────────────────────────────────╮
│ > i want to make a "op" that display a console like                        │
│   terminal with somemenu options and takes input and gives                 │
│   response based on menu selection. get it (it can pipe kbd                │
│   input and screen output in same cli)? and layout of menu                 │
│   shouldn't be hardcoded , it should be read as an external                │
│   file by the op 'layout.txt'                                              │
╰────────────────────────────────

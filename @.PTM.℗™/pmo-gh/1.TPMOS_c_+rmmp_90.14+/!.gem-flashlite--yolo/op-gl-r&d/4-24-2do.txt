# 4-24-2do: Fuzz-Op-GL "Genetic" Alignment Task List

**Goal:** Reach "Genetic" purity in rendering (voxels) and absolute sovereignty in state (camera/xelector).

---

## 1. 8x8x8 Voxel Rendering Engine (Genetic Reconstruction)
- [ ] **Implementation:** Create `draw_voxel_piece()` in `gl_desktop.c`.
- [ ] **Data Structure:** Define an 8x8x8 bitmask pattern for ASCII symbols (the "Piece DNA").
- [ ] **Utility:** Draw the Xelector (XEL) wireframe using this voxel logic for consistent aesthetics.
- **Reference:** `!.gem-flashlite--yolo/#.docs/ascii&gl-vision.txt`

## 2. Xelector (XEL) Sync & Pure Reconstruction
- [ ] **Purge:** Remove legacy `draw_xelector` and local position logic in `gl_desktop.c`.
- [ ] **Reconstruct:** Re-implement XEL visuals to strictly mirror the ASCII-OS pattern (Source of Truth = Sovereign State).
- [ ] **Verification:** Confirm Arrow Keys move the `xlector` piece (Manager) and that the Host's wireframe box follows `xelector_pos_x/y/z` without lag or drift.
- **Reference:** `!.gem-flashlite--yolo/#.docs/gl-fuzz-architecture-blueprint.txt` (Goal 3)

## 3. Finalize Camera Sovereign Piece
- [ ] **Authority:** Ensure `pieces/camera/state.txt` is the ONLY source for the Host's view transformation (X, Y, Z, Pitch, Yaw, Roll).
- [ ] **Cache Mode:** Local Host variables `cam_pos` and `cam_rot` must be read-only caches refreshed from the Manager's sovereign state.
- **Reference:** `projects/fuzz-op-gl/manager/fuzz-op-gl_manager.c` (`sync_camera_piece()`)

## 4. Z-Buffer ASCII Parsing (True 3D Environments)
- [ ] **Parser Update:** Modify `gltpm_parser.c` to detect and load multiple map layers (e.g., `map_01_z0.txt`, `map_01_z1.txt`).
- [ ] **Composition:** Stack these layers spatially into the 3D scene.
- **Reference:** `pieces/apps/gl_os/plugins/gltpm_parser.c` (`gltpm_process_canvas_block`)

## 5. Audit & Compliance
- [ ] **Cleanup:** Remove any remaining `selector` (non-X) references in modular project files.
- [ ] **Final Pass:** Verify all changes against `hard-pass-a24.txt`.

---
*Documented by Gemini CLI - 2026-04-24*

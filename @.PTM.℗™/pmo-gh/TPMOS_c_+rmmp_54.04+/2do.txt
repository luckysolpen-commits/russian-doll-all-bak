TPMOS DEVELOPMENT LOG - April 1, 2026
======================================

[X] 1. Hotkey & Entity Selection Fix (Primary Plan)
    - Restored fuzz-op piece directories from v52.00 reference.
    - Recompiled fuzz-op_manager to use restored structure.
    - Updated manager to seek to end of history file on startup to ignore old keys.
    - Updated run_orchestrator.sh to truncate relay history file on run.

[X] 2. Button Piece Consolidation
    - Moved #.buttons/ to pieces/buttons/ for experimental "button piece" architecture.
    - Updated button.sh, button.ps1, and internal run scripts for new pathing.

[X] 3. Keyboard Modularization ("Two Muscles, One Brain")
    - Goal: Solve Windows "Waiting Bug" (Multi-Key Lag) and achieve 1:1 parity with Linux.
    - Strategy:
        * Created `pieces/keyboard/src/keyboard_input_linux.c` (Direct `/dev/tty` access).
        * Created `pieces/keyboard/src/keyboard_input_win.c` (Direct `CONIN$` access).
        * Refactored `orchestrator.c` to act as the Brain, launching Muscle binaries.
        * Retained separate Joystick specialized input module.
    - Status: COMPLETE. Project-based keyboard input verified.

[ ] 4. Legacy Component Phase-Out
    - Note: Legacy `fuzzpet-app` keyboard response is currently broken. Due to the shift towards "ops-wise" project architecture, we will NOT fix this.
    - Action: Deprecate `pieces/apps/fuzzpet_app`, `pieces/apps/editor`, and `pieces/apps/gl_os`.
    - Action: Update documentation to prioritize `projects/` versions.

[ ] 5. Fondu Architecture Implementation
    - Goal: Create a "production-facing" app installation system and archived "trunk" storage.
    - Strategy: See `fondu.txt` for technical specifications.

  Regarding the compilation errors in +-demo_manager.c, I have analyzed the
  output and will apply the following fixes:
   1. Define Arrow Key Constants: I will replace the use of ARROW_UP and
      ARROW_DOWN with their direct numerical enum values (1002 and 1003).
   2. Fix String Literals: I will ensure all string literals in fprintf
      statements within update_state are properly formed and terminated.
   3. Add Forward Declarations: I will add explicit static forward declarations
      for list_directory_contents, update_state, and build_tree before the main
      function.
   4. Resolve Static Declaration Conflict: I will ensure update_state has a
      consistent static declaration.


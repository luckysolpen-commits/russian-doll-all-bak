BUG REPORT: MAR 3 REFACTOR REGRESSIONS (UPDATE 7)
==============================================
Date: 2026-03-03

1. [RENDERER] ENTITIES DISAPPEARING ON MAP SELECTION
   - Status: CRITICAL
   - Description: When "Control Map" is entered (INTERACT mode), all entities (@, &) and the selector (X) vanish from the map.
   - Root Cause: `map_render.c` is likely failing to read the `on_map` state correctly from the high-speed mirrors (`state.txt`).
   - Investigation finding: In `run_chtpm.sh`, I used `piece_manager` to set states, but `piece_manager` appends a newline/space to some values, or the `get_state_int_fast` function is not robust enough to handle the resulting `state.txt` format.

2. [MANAGER] SELECTOR 'ENTER' FAILURE
   - Status: CRITICAL
   - Description: Selector 'X' is over a pet but pressing ENTER does not take control.
   - Root Cause: Registry iteration in `fuzzpet_manager.c` is likely missing the pieces because of trailing characters in `registry.txt` or coordinate mismatches.

3. [UI] DOUBLE RESPONSE BORDERS
   - Status: MEDIUM
   - Description: There are two `[RESP]` sections appearing in the UI. 
   - Root Cause: `main.chtpm` has a hardcoded RESP line AND the `${piece_methods}` or another variable might be expanding into another RESP line.

ACTION PLAN:
1. Re-initialize `registry.txt` without any extra spaces.
2. Update `get_state_fast` to be extremely robust (trimming all whitespace).
3. Ensure `map_render.c` and `fuzzpet_manager.c` share the same robust logic.
4. Clean `main.chtpm` to ensure only ONE response area exists.

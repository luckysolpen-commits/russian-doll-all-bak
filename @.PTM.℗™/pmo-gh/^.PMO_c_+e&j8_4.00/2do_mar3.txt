2DO MAR 3: DYNAMIC METHODS & SELECTOR INVENTORY
=============================================

1. [PARSER] DYNAMIC METHOD-TO-UI MAPPING
   - Current: Buttons like 'Feed', 'Play' are hardcoded in main.chtpm.
   - Target: The Parser should read the active piece's 'METHOD' section and dynamically render interaction buttons.
   - Update `chtpm_parser.c` to populate a `${piece_methods}` variable or similar logic to generate button tags.

2. [SELECTOR] PIECE EXPANSION
   - Update `selector.pdl` to include: `scan`, `collect`, `place`, `inventory`.
   - Implement `storage_trait.c` to handle 'inventory' as a comma-separated ID list in the selector's state.
   - Implement 'collect' logic: Find entity at cursor -> Set entity `on_map=0` -> Add ID to selector's `inventory` list.
   - Implement 'place' logic: Take first ID from inventory -> Set entity `pos_x/y` to current cursor -> Set entity `on_map=1` -> Remove from inventory.

3. [MANAGER] HOTKEY ROUTING
   - Map numeric keys (2, 3, 4, etc.) to the dynamic methods of the active piece.
   - Example: If Pet 02 is active, '2' = Collect. If Pet 01 is active, '2' = Eat.
   - This ensures the UI remains sovereign to the piece's capabilities.

4. [RENDERER] MAP REGISTRY CLEANUP
   - Ensure `map_render.c` ONLY draws entities where `on_map == 1`.
   - Verify that 'collected' pets correctly vanish from the map.

5. [DASHBOARD] MIRROR SYNC REFINEMENT
   - When Selector is active, clear the pet stats (Hunger/Happiness) or show Selector-specific stats (Inventory count).

6. [UI] EMOJI TOGGLE (LOW PRIORITY)
   - Restore the emoji rendering logic in `map_render.c` using the `emoji_mode` state.

IMPLEMENTATION ORDER:
1. Implement Selector Storage (Traits).
2. Update Manager to route Storage hotkeys.
3. Refactor Parser for Dynamic Buttons.
4. Final verification of 'The Two-Pet Inventory Test'.

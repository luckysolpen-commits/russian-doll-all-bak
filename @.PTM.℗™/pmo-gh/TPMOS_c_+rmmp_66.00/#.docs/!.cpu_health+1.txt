# CPU Health & Throttling Exploration
**Date:** 2026-03-13
**Last Updated:** 2026-03-15
**Author:** Gemini Code Agent
**Objective:** Analyze CPU usage patterns and implement health-preserving strategies.

---

## 0. LEGACY_FUZZPET VALIDATION (CPU-SAFE REFERENCE)

**CRITICAL FINDING:** The `fuzz_legacy_manager.c` implementation represents the **COMPLETE AND FINAL** CPU-safe pattern that all PMO projects should follow.

### 0.1 Why Legacy Fuzzpet is CPU-Safe

The legacy_fuzzpet project has been validated as **completely CPU safe** and **never causes CPU throttle or crashes**. All techniques this framework will ever need are already encompassed in this implementation:

1. **Signal-Safe Process Model:** Uses `fork()/exec()/waitpid()` pattern exclusively—no orphaned processes.
2. **Graceful Main Loop:** Main thread sleeps 1 second (`usleep(1000000)`), input thread handles polling.
3. **Focus-Aware Throttling:** Drops to 100ms sleep when layout is not active.
4. **Efficient History Polling:** Uses `stat()` to check file size before reading; tracks `last_pos` for incremental reads.
5. **Bounded Execution:** All child processes are waited on synchronously—no process accumulation.
6. **No VM Overhead:** Direct binary calls (`move_entity.+x`, `zombie_ai.+x`) instead of VM interpretation.

**VERDICT:** Legacy fuzzpet techniques already encompass all of the techniques this framework will ever need to use, and they **never cause CPU throttle or crash**. Future agents should treat this as the canonical reference implementation.

---

## 1. ANALYSIS OF CURRENT PATTERNS

### 1.1 Legacy Pattern (fuzz_legacy_manager.c)
- **Mechanism:** Main loop with `usleep(16667)` (approx. 60 FPS) in input thread; main thread sleeps 1 second.
- **Efficiency:** Uses `stat()` to check for history file changes before opening it.
- **Throttling:** Implements `if (!is_active_layout()) { usleep(100000); continue; }` which drops CPU usage by 6x when the project is not in focus.
- **CPU Tax:** Launches `move_entity.+x` and `zombie_ai.+x` via `fork/exec/waitpid`. This is relatively heavy but only occurs on input.
- **Signal Handling:** Inherits default signal handling—Ctrl+C kills process cleanly.

### 1.2 Editor Module Pattern (editor_module.c)
- **Mechanism:** Single-threaded main loop with `usleep(16667)` polling.
- **Efficiency:** Uses `stat()` for history file monitoring; tracks `last_pos` for incremental reads.
- **Throttling:** Implements `if (!is_active_layout()) { usleep(100000); continue; }` for focus-aware sleep.
- **CPU Tax:** Uses `system()` for external calls (`render_map.+x`, `place_tile.+x`, `move_entity.+x`).
- **CRITICAL ISSUE:** No explicit signal handler. On Ctrl+C, may leave child processes orphaned if killed mid-`system()` call.

### 1.3 Ops Pattern (man-ops_module.c)
- **Mechanism:** Similar to Legacy, using `usleep(16667)` and `stat()` polling.
- **Efficiency:** Updates UI state (`write_ops_state`) and triggers render only when `processed == 1`.
- **Throttling:** Same "Inactive Sleep" (100ms) when layout is not active.
- **CPU Tax:** Calls Shared Traits (`move_entity.+x`) directly via `system()`.

### 1.4 PAL Pattern (man-pal_module.c)
- **Mechanism:** Relay module that launches `prisc+x` (VM) to process input.
- **Efficiency:** Same "Inactive Sleep" (100ms) when layout is not active.
- **CPU Tax (CRITICAL):** Launches the Prisc VM for **EVERY SINGLE KEY** in the history buffer. If multiple keys are pressed, multiple VM instances are launched sequentially via `system()`. This is significantly more taxing than direct trait calls.

### 1.5 Prisc VM Pattern (prisc+x.c)
- **Mechanism:** Interprets custom assembly; launches external binaries via `exec_custom_op()`.
- **Signal Handling:** Has `handle_sigint()` that calls `_exit(130)` for immediate termination.
- **CPU Tax:** Can spawn multiple processes rapidly if not properly controlled by calling module.
- **Process Cleanup:** Relies on parent process to clean up; no internal process group management.

---

## 2. CPU HEALTH RECOMMENDATIONS

### 2.1 The "Active Pulse" Strategy
Future projects should move away from high-frequency polling when idle. 
- **Idle State:** 100ms - 500ms sleep.
- **Active State (Input detected):** 16ms sleep (60 FPS) for a short burst (Pulse), then back to Idle.

### 2.2 Prisc VM Optimization (Self-Killing Mandate)
- **Problem:** `prisc+x` is a full VM. Launching it repeatedly is the primary source of throttling issues.
- **Fix:** `prisc+x` must be "Self-Killing". The `halt` instruction correctly sets `pc = inst_count`, terminating the loop.
- **Optimization:** Modify `man-pal_module.c` to launch `prisc+x` **ONCE** per history update, passing ALL keys as an argument or allowing `prisc+x` to consume the entire buffer in one go.

### 2.3 The "PAL Fix"
- **Implementation:** Ensure `prisc+x` always hits the `halt` path.
- **Validation:** Add `exit(0)` explicitly at the end of `prisc+x` to ensure OS-level cleanup.
- **Integration:** Use `system(cmd)` for synchronous execution but consider `&` and manual `wait` if parallel non-blocking execution is needed (though sequential is safer for state consistency).

### 2.4 The "Editor Fix" (NEW - CRITICAL FOR CPU SAFETY)
- **Problem:** `editor_module.c` lacks signal handling. On Ctrl+C, child processes started via `system()` may become orphaned and continue running, causing CPU spikes and crashes.
- **Fix:** Add explicit signal handler:
  ```c
  void handle_sigint(int sig) {
      // Kill any child processes here if tracking PIDs
      _exit(130);
  }
  // In main(): signal(SIGINT, handle_sigint);
  ```
- **Better Fix:** Use `fork()/exec()/waitpid()` pattern like `fuzz_legacy_manager.c` instead of `system()` for critical operations.

### 2.5 Process Group Management (NEW)
- **Problem:** Modules using `system()` or `fork()/exec()` without proper process group management can leave orphaned processes on abrupt termination.
- **Fix:** Set process group leader in main:
  ```c
  setpgid(0, 0);  // Make this process a group leader
  ```
- **Cleanup:** On exit or signal, kill entire process group:
  ```c
  kill(-getpgrp(), SIGTERM);  // Negative PID kills process group
  ```

---

## 3. FUTURE PROJECT STANDARDS (CPU COMPLIANCE)

1. **Focus Throttling:** ALWAYS use `is_active_layout()` to sleep longer when not in focus.
2. **Stat-First Polling:** Never open state/history files unless `stat()` confirms they have changed (`st_mtime` or `st_size`).
3. **VM Batching:** If using Prisc, batch operations. Do not launch the VM in a tight loop.
4. **Signal Handling:** ALL modules MUST have explicit `SIGINT` handlers to ensure clean shutdown.
5. **Process Group Management:** ALL modules MUST set process group and clean up on exit.
6. **Fork/Exec/Waitpid Pattern:** Prefer this over `system()` for better process control (see `fuzz_legacy_manager.c`).
7. **Legacy Reference:** When in doubt, mirror the `fuzz_legacy_manager.c` pattern—it is validated CPU-safe.

---

## 4. CPU HEALTH MONITORING

### 4.1 Quick Health Check
```bash
# Check for runaway PMO processes
ps aux | grep -E "\.\+x|prisc\+x" | grep -v grep
```

### 4.2 Process Count Threshold
- **Normal:** 0-5 PMO processes (active modules)
- **Warning:** 6-15 processes (possible accumulation)
- **Critical:** 16+ processes (runaway fork bomb—run `kill_all.sh` immediately)

### 4.3 CPU Usage Threshold
- **Normal:** <5% per module
- **Warning:** 5-20% (check for tight loops)
- **Critical:** >20% (immediate investigation required)

---

*Status: Legacy Fuzzpet validated as CPU-SAFE reference. Editor module signal handling identified as crash source. Process group management added as mandatory standard.*

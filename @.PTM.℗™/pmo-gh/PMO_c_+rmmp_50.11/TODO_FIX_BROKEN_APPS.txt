# TODO.TXT - Restore Working Code While Preserving New Features
**Date:** 2026-03-25
**Priority:** CRITICAL - System-wide breakage from "ops vs apps" refactor

---

## THE PROBLEM

Someone messed around with "ops vs apps" architecture and broke:
1. **op-ed** - Shows "PROJECT MANAGER" instead of "O P - E D   A D V A N C E D"
2. **fuzzpet/fuzz-op** - Likely broken initialization
3. **Other projects** - template, demo_1, etc. may be affected

**What's broken:** Missing app-level state initialization in `run_chtpm.sh`

**What to preserve:** 
- cli-io modifications (the `${cli_input}` variable)
- Any accumulation logic that was added
- User Auth app button
- RMMP Editor button

---

## TASK 1: Fix run_chtpm.sh Initialization

### 1a. Add op-ed app-level initialization

**ADD after the existing editor initialization (around line 70-80):**

```bash
# ============================================================================
# OP-ED APP STATE (CRITICAL - was missing, causes broken UI)
# ============================================================================

# op-ed App State
mkdir -p pieces/apps/op-ed
echo "gui_focus=1" > pieces/apps/op-ed/state.txt
echo "emoji_mode=0" >> pieces/apps/op-ed/state.txt
echo "cursor_x=5" >> pieces/apps/op-ed/state.txt
echo "cursor_y=5" >> pieces/apps/op-ed/state.txt
echo "cursor_z=0" >> pieces/apps/op-ed/state.txt
echo "last_key=None" >> pieces/apps/op-ed/state.txt
echo "project_id=op-ed" >> pieces/apps/op-ed/state.txt
echo "map_idx=0" >> pieces/apps/op-ed/state.txt
echo "glyph_idx=0" >> pieces/apps/op-ed/state.txt

# op-ed Manager State
mkdir -p pieces/apps/op-ed/manager
echo "project_id=op-ed" > pieces/apps/op-ed/manager/state.txt
echo "active_target_id=selector" >> pieces/apps/op-ed/manager/state.txt
echo "current_z=0" >> pieces/apps/op-ed/manager/state.txt
echo "last_key=None" >> pieces/apps/op-ed/manager/state.txt

# op-ed Selector Piece (App-level mirror)
mkdir -p pieces/apps/op-ed/pieces/selector
echo "pos_x=5" > pieces/apps/op-ed/pieces/selector/state.txt
echo "pos_y=5" >> pieces/apps/op-ed/pieces/selector/state.txt
echo "pos_z=0" >> pieces/apps/op-ed/pieces/selector/state.txt
echo "active=1" >> pieces/apps/op-ed/pieces/selector/state.txt
echo "icon=X" >> pieces/apps/op-ed/pieces/selector/state.txt

# op-ed History/View Files
> pieces/apps/op-ed/history.txt
> pieces/apps/op-ed/view.txt
> pieces/apps/op-ed/view_changed.txt
> pieces/apps/op-ed/response.txt
```

### 1b. Verify fuzzpet_app initialization

**Current run_chtpm.sh already has fuzzpet_app init, but verify it includes:**

```bash
# This should already exist around line 95-105
mkdir -p pieces/apps/fuzzpet_app/fuzzpet
echo "name=Fuzzball" > pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "hunger=50" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "happiness=55" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "energy=100" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "level=1" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "pos_x=5" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "pos_y=2" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "pos_z=0" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "status=active" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
echo "emoji_mode=0" >> pieces/apps/fuzzpet_app/fuzzpet/state.txt
```

### 1c. Add template project initialization (for player_app default)

**ADD after op-ed initialization:**

```bash
# ============================================================================
# TEMPLATE PROJECT STATE (for player_app default loading)
# ============================================================================

mkdir -p projects/template/manager
echo "project_id=template" > projects/template/manager/state.txt
echo "active_target_id=hero" >> projects/template/manager/state.txt
echo "current_z=0" >> projects/template/manager/state.txt
echo "last_key=None" >> projects/template/manager/state.txt
> projects/template/history.txt

# Template hero piece
mkdir -p projects/template/pieces/hero
echo "name=Hero" > projects/template/pieces/hero/state.txt
echo "type=hero" >> projects/template/pieces/hero/state.txt
echo "pos_x=5" >> projects/template/pieces/hero/state.txt
echo "pos_y=5" >> projects/template/pieces/hero/state.txt
echo "pos_z=0" >> projects/template/pieces/hero/state.txt
echo "on_map=1" >> projects/template/pieces/hero/state.txt
```

---

## TASK 2: Fix os.chtpm (Preserve cli-io, Fix GL-OS launch)

**Current os.chtpm has cli-io but wrong GL-OS launch method**

**File:** `pieces/chtpm/layouts/os.chtpm`

**CHANGE this line:**
```xml
<button label="GL-OS" href="pieces/chtpm/layouts/desktop.chtpm" />
```

**TO:**
```xml
<button label="GL-OS" onClick="LAUNCH:GL-OS" />
```

**KEEP these lines (they are NEW features to preserve):**
```xml
<button label="User Auth" href="pieces/apps/user/layouts/user.chtpm" />
<button label="RMMP Editor" href="pieces/apps/op-ed/layouts/op-ed.chtpm" />
```

**CHANGE cli_io line:**
```xml
<cli_io label="${cli_input}" />
```

**TO (restore empty default):**
```xml
<cli_io label="" />
```

---

## TASK 3: Verify/Restore Other Broken Projects

### 3a. Check demo_1 project

```bash
# Check if demo_1 has manager state
ls -la projects/demo_1/manager/

# If missing, create it:
mkdir -p projects/demo_1/manager
echo "project_id=demo_1" > projects/demo_1/manager/state.txt
echo "active_target_id=hero" >> projects/demo_1/manager/state.txt
echo "current_z=0" >> projects/demo_1/manager/state.txt
echo "last_key=None" >> projects/demo_1/manager/state.txt
```

### 3b. Check man-* projects (man-pal, man-ops, man-add)

These are APPS not projects, so they need pieces/apps/ initialization:

```bash
# Check if they have manager state
ls -la pieces/apps/man-pal/manager/
ls -la pieces/apps/man-ops/manager/
ls -la pieces/apps/man-add/manager/

# If missing, they may need initialization added to run_chtpm.sh
```

---

## TASK 4: Compile and Test

### 4a. Compile all modules
```bash
./compile_all.sh
```

### 4b. Test sequence
```bash
# 1. Launch runtime
./run_chtpm.sh

# 2. Test op-ed
# From main menu, navigate to "RMMP Editor" (option 9)
# Expected: "O P - E D   A D V A N C E D" UI with map

# 3. Test fuzzpet
# From main menu, navigate to "Launch Legacy FuzzPet"
# Expected: Fuzzpet launcher or game UI

# 4. Test project loader
# From main menu → Project Manager → PLAYR Player
# Expected: Project list with working selections
```

---

## TASK 5: Document What Was Preserved

After fixing, verify these NEW features still work:

1. **cli-io variable** - Check `${cli_input}` is still in os.chtpm
2. **User Auth button** - Still visible in main menu
3. **RMMP Editor button** - Still visible in main menu
4. **Any accumulation logic** - Check if there were any accumulator mods to preserve

---

## REFERENCE FILES

Compare current vs reference:
- **run_chtpm.sh** - Current vs `#.ref/PMO_c_+rmmp_41.01/run_chtpm.sh`
- **os.chtpm** - Current vs `#.ref/PMO_c_+rmmp_41.01/pieces/chtpm/layouts/os.chtpm`
- **op-ed.chtpm** - Current vs `#.ref/PMO_c_+rmmp_41.01/pieces/apps/op-ed/layouts/op-ed.chtpm`

---

## EXECUTION ORDER

1. **FIRST:** Backup current files
   ```bash
   cp run_chtpm.sh run_chtpm.sh.broken.$(date +%Y%m%d)
   cp pieces/chtpm/layouts/os.chtpm pieces/chtpm/layouts/os.chtpm.broken.$(date +%Y%m%d)
   ```

2. **SECOND:** Apply run_chtpm.sh fixes (Task 1)

3. **THIRD:** Apply os.chtpm fixes (Task 2)

4. **FOURTH:** Add missing project initializations (Task 3)

5. **FIFTH:** Compile and test (Task 4)

6. **SIXTH:** Document preserved features (Task 5)

---

## SUCCESS CRITERIA

- [ ] op-ed launches with correct "O P - E D   A D V A N C E D" UI
- [ ] Selector (X) visible on map and moves with arrow keys
- [ ] fuzzpet/fuzz-op launches and pet is visible
- [ ] Project loader shows all projects
- [ ] cli-io still works (if it was working before)
- [ ] User Auth button still in main menu
- [ ] RMMP Editor button still in main menu
- [ ] GL-OS launches correctly
- [ ] No compilation errors

---

**END OF TODO.TXT**

# FUZZPET OP & PAL COMPLIANCE - M11
Goal: Achieve full functional parity with legacy FuzzPet using TPM and PAL.

## 1. SELECTOR & INTERACT MODE
- **Issue:** Selector in Control Map is not triggering 'interactive mode'.
- **Status:** ✅ Fixed. Added `id="map"` to button and updated `chtpm_parser.c` to capture focus.
- **PAL Fix:** ✅ Done. `game_loop.asm` now handles `selector_input` branch.
- **TPM Compliance:** ✅ Done. Selector piece state is synced in `projects/fuzzpet_v2/pieces/selector`.

## 2. REMAINING SELECTOR METHODS
- [x] **Scan:** `scan_op.+x` - ✅ Done.
- [x] **Collect:** `collect_op.+x` - ✅ Done.
- [x] **Place:** `place_op.+x` - ✅ Done.
- [x] **Inventory:** `inventory_op.+x` - ✅ Done.

## 3. NAV MENU RESPONSES
- [x] Update `render_map.c` to extract messages. ✅ Done.
- [x] Ensure ops hit `frame_changed.txt`. ✅ Done.

## 4. DEMO-1 COMPATIBILITY
- [x] Fix path resolution in `playrm_module.c`. ✅ Done.
- [x] Generic `move_entity` support. ✅ Done.

## 5. PERFORMANCE & CPU
- Always use the `prisc` binary for logic to keep things modular.
- Use `frame_changed.txt` marker file to avoid unnecessary re-renders.
- Ensure the `playrm_module` pulse loop respects the 60fps cap (16.6ms sleep).

## 6. PROJECT ENCAPSULATION (V2)
- [x] Project-Local History: `projects/fuzzpet_v2/history.txt`
- [x] Project-Local Module: `projects/fuzzpet_v2/manager/fuzzpet_v2_module.c`
- [x] Project-Local State: `projects/fuzzpet_v2/manager/state.txt`
- [x] Legacy Isolation: `main.chtpm` restored to original `fuzzpet_manager`.
- [x] Launcher Parity: Supports both legacy reference and V2 PAL branches.

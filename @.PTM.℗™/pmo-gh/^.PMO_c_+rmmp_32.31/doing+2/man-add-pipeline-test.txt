# PROJECT: man-add (OP-ED Style Pipeline Test)
**Purpose:** Debug the input pipeline using the proven `op-ed` pattern.

## PIPELINE

```
keyboard → parser → player_app/history.txt → man-add_module → state.txt → render
```

## TEST STEPS
1.  **Launch System:** `./run_chtpm.sh`
2.  **Select Project:** Navigate to "Project Manager" -> "PLAYR Player" -> "man-add"
3.  **Verify Layout:** (ROUTING COMPLETED - USER VERIFIED)
4.  **Verify Module:** (ROUTING COMPLETED - USER VERIFIED)
5.  **Enter Interact:** Select "Control Map" and press ENTER.
6.  **Move:** Press arrow keys.
7.  **Investigate Key Path:** If `[KEY]` is "None", run `./check_history.sh` to see where keys are landing.
    - If keys are in `projects/man-add/history.txt`, the module must be updated to read from there.
8.  **Verify Movement:** Selector 'X' should move on map once module reads the correct path.

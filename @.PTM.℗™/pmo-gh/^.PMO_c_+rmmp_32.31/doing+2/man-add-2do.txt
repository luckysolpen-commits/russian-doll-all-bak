# MAN-ADD PROJECT TODO - Minimal Pipeline Test (OP-ED Pattern)
**Date:** 2026-03-12
**Goal:** Prove parser writes, module reads. Nothing else.

---

## 0. HOW PROJECT SELECTION WORKS (RESEARCH DATA)
To ensure selecting `man-add` directs to the correct layout and module, the following mechanism is used (following the `op-ed` pattern):

1.  **Project Location:** `projects/man-add`
2.  **App Location:** `pieces/apps/man-add`
3.  **Layout Location:** `pieces/apps/man-add/layouts/man-add.chtpm`
4.  **Module Location:** `pieces/apps/man-add/plugins/man-add_module.c`
5.  **Input Pipeline:** Uses `pieces/apps/player_app/history.txt` in INTERACT mode.
6.  **State Sync:** Writes to `pieces/apps/player_app/state.txt`.

---

## PHASE 1: CREATE PROJECT STRUCTURE

### 1.1 Create Project Directory
```bash
mkdir -p projects/man-add/maps projects/man-add/pieces/selector
```

### 1.2 Create App Directory
```bash
mkdir -p pieces/apps/man-add/layouts pieces/apps/man-add/plugins/+x
```

### 1.3 Create project.pdl
```bash
cat > projects/man-add/project.pdl << 'EOF'
SECTION      | KEY                | VALUE
----------------------------------------
META         | project_id         | man-add
META         | title              | Man Add Project
META         | starting_map       | map_0001
STATE        | active             | 1
EOF
```

---

## PHASE 2: CREATE LAYOUT

### 2.1 Create man-add.chtpm
Located at: `pieces/apps/man-add/layouts/man-add.chtpm`
Matches `op-ed` style layout.

---

## PHASE 3: CREATE MODULE

### 3.1 Create man-add_module.c
Located at: `pieces/apps/man-add/plugins/man-add_module.c`
Reads from `pieces/apps/player_app/history.txt`.
Writes to `pieces/apps/player_app/state.txt`.

### 3.2 Compile
```bash
gcc -o pieces/apps/man-add/plugins/+x/man-add_module.+x pieces/apps/man-add/plugins/man-add_module.c -lpthread
```

---

## PHASE 4: UPDATE PARSER
Already updated `load_vars` and `send_command` to route `man-add` to its new layout and module.

---

## CURRENT STATUS (2026-03-12)
- [x] Project "man-add" appears in Loader
- [x] Selecting "man-add" redirects to layout (verified by USER)
- [x] Key injection to `pieces/apps/player_app/history.txt` verified.
- [x] `chtpm_parser.c` fixed to load `player_app/state.txt` for `man-add`.
- [x] RACE CONDITION FIXED: Module now overwrites `render_map` stale state.
- [x] High-signal debug info (Heartbeat, Raw Key, Updates) active in UI.

---

## FINAL VERIFICATION
`check_history.sh` confirms:
- `project_id=man-add`
- `last_raw_key` updates in real-time.
- `selector_x/y` synced across all state files.

---

## SUCCESS CRITERIA
- [x] Project "man-add" appears in Loader
- [x] Selecting "man-add" loads `man-add.chtpm`
- [x] Module `man-add_module` launches
- [x] Arrow keys move selector in INTERACT mode
- [x] [KEY] and [POS] update correctly (after race condition fix)

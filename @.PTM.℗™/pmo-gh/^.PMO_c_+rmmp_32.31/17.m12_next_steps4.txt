                        
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
 > do u understand? ; rmmp-editor uses legacy move selector "op-ed" doesn't     
   have move selector at all. also we wanted to prove the "op based pipeline    
   first in " man-ops" then prove "pal pipeline" in "pal ops" before we fix op  
   ed and fuzzpet_v2, do u understand this as our next steps ? if so write      
   17.m12_next_steps4.txt      
   
   (also fuzz_legacy isn't working the same as actual legacy (and actual legacy lost collision detection?)                                                   
▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄

# 17.m12_next_steps4.txt - The Proven Pipeline Strategy
# Goal: Prove C-Ops first, then PAL-Ops, then scale to Editor/V2.

## 1. TRACK 1: MAN-OPS (PROVE C-OP PIPELINE)
- **Status:** PENDING REFACTOR
- **Goal:** Move selector using direct C calls to ops.
- **Files:** `pieces/apps/man-ops/plugins/man-ops_module.c`
- **What to do:** 
    - Refactor to poll `pieces/apps/player_app/history.txt`.
    - Handle Arrow Keys (1000-1003) and WASD.
    - Call `move_selector.+x <dir> man-ops` directly via `system()`.
    - Sync state from `projects/man-ops/pieces/selector/state.txt`.
- **Why:** This proves the shared history buffer and project-aware ops are 100% stable.

## 2. TRACK 2: MAN-PAL (PROVE PAL PIPELINE)
- **Status:** PENDING IMPLEMENTATION
- **Goal:** Move selector using PAL scripts that call ops.
- **Files:** `pieces/apps/man-pal/plugins/man-pal_module.c`, `projects/man-pal/scripts/move.asm`
- **What to do:**
    - C Module polls `pieces/apps/player_app/history.txt`.
    - For every key, call `prisc+x move.asm` passing the key as an environment variable (`PRISC_KEY`).
    - `move.asm` logic: if key == 1002, `exec ./move_selector.+x up man-pal`.
- **Why:** This proves the Prisc VM can correctly orchestrate ops in a project-aware context.

## 3. TRACK 3: FUZZ_LEGACY (PRESERVE SUCCESS)
- **Status:** ✅ VALIDATED
- **Goal:** Use the already-working `move_entity` logic for legacy entities.
- **Why:** This remains our safety net - it uses the most stable movement trait.

## 4. TRACK 4: SCALE TO COMPLEXITY (OP-ED & FUZZPET_V2)
- **Status:** PENDING (Wait for Tracks 1 & 2)
- **Goal:** Once Tracks 1 & 2 prove the pipelines, refactor these large modules.
- **Op-Ed:** Will adopt the Track 1/2 patterns for advanced editing.
- **FuzzPet_V2:** Will adopt Track 2 pattern for user-moddable pet behavior.

"Prove the C-Op muscle. Prove the PAL-Op brain. Then build the body."

# mirror_status_py.txt - Python PMO Progress Report

**Document Type:** Status Report & Technical Handover
**Objective:** Track the alignment of Python Build (`PHTMP+GL&FUZ_0.04/`) with C Build (`!.PMO_00.08/`).

---

## 1. COMPLETED ALIGNMENT (The Wins)

### 1.1 Macro Folder Sovereignty (MATCHED)
*   Legacy pieces (`fuzzpet`, `world`, `clock`) have been migrated into `pieces/apps/fuzzpet_app/`.
*   All executables and scripts now use the Macro Folder paths.

### 1.2 TPM-WISE Path Resolution (MATCHED)
*   `location_kvp` and `path_utils.py` implemented.
*   The system no longer uses hardcoded relative paths; it resolves its own reality.

### 1.3 Option Z: Sovereign Composite (MATCHED)
*   `fuzzpet.py` acts as the **Stage Producer**, writing logic content to `view.txt`.
*   `phtpm_parser.py` acts as the **Theater Composer**, substituting variables and nesting the `view.txt` content into the UI shell.

### 1.4 Single Pulse Synchronization (MATCHED)
*   `mtime` polling has been replaced by **File Size Tallies**.
*   All rendering components (`parser`, `renderer`, `gl_renderer`) monitor `frame_changed.txt` for sub-millisecond sync.

---

## 2. REMAINING DISCREPANCIES (The Gap)

### 2.1 UI Layout Parity (IN PROGRESS)
*   **Status:** `os.chtpm` and `main.chtpm` have been updated, but fine-tuning of spacing and "Theater Boxes" (borders) is needed to match the C CLI aesthetics exactly.
*   **Task:** Verify all variables (`${fuzzpet_hunger}`, etc.) map 1:1 with the C version's Mirror names.

### 2.2 Input Relay Calibration (IN PROGRESS)
*   **Status:** Logic keys (2=Feed, 3=Play, etc.) are aligned in `fuzzpet.py`.
*   **Task:** Verify that `INTERACT` mode in `phtpm_parser.py` correctly silences the OS nav and yields full relay sovereignty to the Module without glitching.

---

## 3. TECHNICAL NOTES FOR NEW CONTEXT
*   **The Run Script:** `run_pmo.py` is the entry point. It populates `location_kvp` before launching the orchestrator.
*   **The Pulse:** If the UI isn't updating, check if `pieces/display/frame_changed.txt` is being appended to by the writing process.
*   **Import Depth:** Modules in `pieces/apps/fuzzpet_app/fuzzpet/plugins/` need 4 levels of `sys.path` injection to reach the root.

## 1. COMPLETED ALIGNMENT (MATCHED)
*   **Surgical Rendering:** Element-based tokenizer eliminates tag leakage and controls whitespace.
*   **Pure Focus Navigation:** Verified focus-only digit jumping; Enter required for activation.
*   **Perfect Aesthetics:** Visual twin of the C version achieved.

---
**Status:** 100% ALIGNED (Twin achieved)
**Standard:** PMO v1.4.0

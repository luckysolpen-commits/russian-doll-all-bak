
@
its creating corrupt files in main when selector moves entity (must stop), 
and its taxing cpu with some process thats running 2 fast 2 often 
60fps, on change , or second clock should be the only things running , 
and they have to be kept in check ; lets scan the codebase and make sure theires
no errant process. we dont need to do anything fancy , just try 2 locate
any runaway loops for now. lets document this investigation and solution process and attempts in ?.corrupt&scan.txt 


────────────────────────────────────────────────────────────────────────────╯
✦ location_kvp has an outdated project_root, which is likely causing
  piece_manager.c to fail when writing state files. More importantly, I've
  confirmed that clock_daemon is triggering a heavy I/O chain (Clock -> Display
  Pulse -> Parser -> Master Ledger Pulse -> Renderer) three times a second. While
  1 FPS shouldn't be "high CPU," the repeated forking and I/O are inefficient. I'm
  still puzzled by the corrupt D-] filenames and will now check orchestrator.c for
  any missing sleeps or other issues.



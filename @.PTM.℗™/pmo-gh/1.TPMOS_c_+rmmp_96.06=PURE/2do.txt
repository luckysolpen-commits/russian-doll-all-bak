# TODO: Manager State Sovereignty & Input Handling Fixes

## Goal:
Stabilize manager's control over camera and map states, ensuring correct input parsing and state persistence.

## Specific Tasks for fuzz-op-gl_manager.c:

1.  **Update `is_map_control` Logic:**
    *   Remove toggle behavior for `INTERACT` and `OP fuzz-op-gl::toggle_map_mode`.
    *   Implement absolute setting: `INTERACT` command sets `is_map_control = 1;`.
    *   Implement absolute setting: `ESC` command sets `is_map_control = 0;`.

2.  **Refactor `gltpm_input_thread` Camera Handling:**
    *   Remove direct local handling of camera mode keys ('1'-'4') and camera fly/pan keys (WASD/ZXQE/Q/E).
    *   Ensure Manager parses injected `CAMERA_MODE:<n>` and `CAMERA_MOVE:<dx>,<dy>,<dz>` commands from history.
    *   Confirm Arrow keys continue to inject `KEY_PRESSED` events for Xelector movement.

3.  **Verify `save_manager_state()`:**
    *   Ensure `camera_mode`, `cam_pos`, `cam_rot` are correctly saved along with other state variables.

## CPU Safety & Lifecycle (DONE):

1.  **Update `pieces/os/kill_all.sh`:** (DONE)
    *   Add `surgical_kill "fuzz-op-gl_manager"`
    *   Add `surgical_kill "gl_desktop"`
    *   Add `surgical_kill "move_entity"` and other ops.

2.  **Harden `fuzz-op-gl_manager.c`:** (DONE)
    *   Add `setpgid(0, 0)` to `main()` to ensure it leads its own process group.

3.  **Harden `gl_desktop.c`:** (DONE)
    *   Add `setpgid(0, 0)` to `main()`.

## Method:
Use `default_api.replace` for targeted text modifications, ensuring precise context matching.

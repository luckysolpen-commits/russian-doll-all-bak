# PMO Development TODO List
**Last Updated:** 2026-03-15
**Project:** fuzz_legacy + PDL Implementation

---

## ✅ COMPLETED

### Phase 1: Basic Functionality (DONE)
- [x] Entity rendering (selector, pet, zombie)
- [x] Shadow selector sync (follows entity when selected)
- [x] Selector renders BEHIND entity (hides when on same tile)
- [x] Selector passes through obstacles when alone
- [x] Zombie chase AI
- [x] Zombie can't be selected
- [x] Zombie collision avoidance (won't move onto entity tiles)

### Phase 2: Z-Level Support (DONE)
- [x] Z-level change with 'x' (up) and 'z' (down) keys
- [x] Map changes based on Z-level (surface → underground → sky)
- [x] Z-level display in UI (`[Z-LEVEL]: X`)
- [x] Frame deduplication (only render when view changes)
- [x] Z-level persistence via piece_manager (matches fuzzpet_app)

### Phase 3: map_id Association (DONE)
- [x] `map_id` field in piece state
- [x] `create_piece.c` records current_map when creating pieces
- [x] `render_map.c` filters pieces by `map_id`
- [x] Selector inherits entity's `map_id` on select

### Phase 4: PDL Reader Implementation (DONE)
- [x] Create `pieces/system/pdl/pdl_reader.c`
  - [x] `get_method(piece_id, event)` - Get handler for event
  - [x] `list_methods(piece_id)` - List all methods
  - [x] `has_method(piece_id, event)` - Check if method exists
- [x] Compile PDL reader as standalone binary
- [x] Update `compile_all.sh` to include PDL reader

---

## 🚧 IN PROGRESS

### Phase 4: PDL Reader Implementation
- [ ] Create `pieces/system/pdl/pdl_reader.c`
  - [ ] `get_method(piece_id, event, out_handler)` - Get handler for event
  - [ ] `list_methods(piece_id, out_list)` - List all methods
  - [ ] `has_method(piece_id, event)` - Check if method exists
- [ ] Create `pieces/system/pdl/pdl_reader.h`
- [ ] Compile PDL reader as shared utility
- [ ] Update `compile_all.sh` to include PDL reader

---

## 📋 PENDING

### Phase 5: Hotkey/Method System (fuzz_legacy)
**CRITICAL REQUIREMENT:** Hotkeys (2-8) must use the SELECTED ENTITY's methods, NOT selector's methods.

- [ ] Add method key handling (keys 2-8) to `fuzz_legacy_manager.c`
  - [ ] Check if `active_target_id != "selector"`
  - [ ] Call `get_method_name(active_target_id, key_num, out_method)`
  - [ ] Execute method from selected entity's PDL
- [ ] Implement `get_method_name()` using PDL reader (NOT piece_manager for performance)
- [ ] Wire up method execution:
  - [ ] `scan`, `collect`, `place`, `inventory` → storage ops
  - [ ] `toggle_emoji` → toggle emoji mode
  - [ ] `toggle_clock` → toggle clock mode
  - [ ] `feed`, `sleep`, etc. → execute and log response with irregular verbs
- [ ] Update UI layout to show method buttons dynamically

### Phase 6: Dynamic Method UI
- [ ] Update `game.chtpm` layout to read methods from piece
- [ ] Display method buttons (2-8) based on piece's PDL
- [ ] Map key presses (2-8) to method execution

### Phase 7: Z-Level Persistence (Match fuzzpet_app)
**CRITICAL REQUIREMENT:** Z-level changes must persist exactly like fuzzpet_app.

- [ ] Use `piece_manager.+x` to update pos_z (NOT direct file write)
- [ ] Update BOTH active entity AND selector pos_z
- [ ] Hit `frame_changed.txt` with 'Z' marker after Z change
- [ ] Verify pos_z persists across reloads
- [ ] Remove duplicate pos_z lines from state files

### Phase 8: PDL Writing (Dynamic Skills)
- [ ] Create `add_method(piece_id, event, handler)` - Add method to PDL
- [ ] Create `remove_method(piece_id, event)` - Remove method from PDL
- [ ] Implement "learn skill" functionality
- [ ] Implement "forget skill" functionality

### Phase 9: Other Projects
- [ ] Apply same fixes to `fuzzpet_v2`
- [ ] Apply same fixes to `editor_module`
- [ ] Apply same fixes to `man-ops`

---

## 🔍 KNOWN ISSUES

1. **Z-level not persisting** - Need to use piece_manager like fuzzpet_app
2. **Multiple pos_z lines** - Sometimes selector state gets duplicate pos_z= lines
3. **Frame deduplication** - May need to improve comparison logic for edge cases

---

## 📝 REFERENCE FILES

- `pieces/apps/fuzzpet_app/manager/fuzzpet_manager.c` - Reference for:
  - Z-level changes (lines ~280-310)
  - Method key handling (lines ~330-400)
  - `get_method_name()` implementation (line 205)
- `pieces/master_ledger/plugins/piece_manager.c` - PDL parsing and method listing
- `#.docs/!.world_architecture.txt` - Architecture documentation
- `#.docs/state-vs-pdl.txt` - State vs PDL analysis
- `#.docs/!.map_z_fix.txt` - Z-level fix documentation

---

## 🎯 NEXT STEPS

1. **Fix Z-level persistence** (Phase 7) - Match fuzzpet_app pattern exactly
2. **Implement PDL reader** (Phase 4) - Core infrastructure for method dispatch
3. **Add hotkey support to fuzz_legacy** (Phase 5) - Wire up keys 2-8 with SELECTED ENTITY methods
4. **Test method execution** - Verify methods from PDL are called correctly
5. **Update UI** - Show dynamic method buttons

---

*Last major milestone: Z-level map changing is WORKING! Press 'x' to go up (underground `U` walls), 'z' to go down (surface `.`).*

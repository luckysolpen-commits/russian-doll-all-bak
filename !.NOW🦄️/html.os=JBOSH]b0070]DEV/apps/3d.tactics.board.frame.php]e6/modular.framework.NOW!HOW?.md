

start breaking shit out now , we didn't know how 
2 test php local befor 4 sure with godaddy 
so we couldn't get anywhere 


we dont want to hardcode data going forward

ie : break out all peice position 
to a seperate file.csv read them from php 
and write to that file each move. 
(later we will allow save and load of this file
from top of the window menu bar "file" button)

then lets write a script will run a php server
that mirrors my go daddy server

keep health etc in peices file (like wsr structure)




(later we will use cached voxel sculptures.)

break out the move/turn number to a data file. 

populate peices from a peice bank list

<we should even break out all functions into txt
and trigger them from a php file "if peice calls it"> 
<then peices can reuse them>


(all of this will go in chtgl after we do it here first
do it here first just trust me .
do entire thing here first, 
forget about chtrm/chtml cept 4 principles
forget completely about inner js 4 NOW(later is ok)


😜️use evals 4 the functions, u may still even have
example of this ; its a plugin>>
doesn't have to be an eval tho. just seperate .js is fine; 
but use a .js bank; thats what rmmv does i think. 
this allows then to turn plugins on or off😜️






TODO List: Bring Multi-Threaded Architecture up to Speed with AR Functionality

Current Status:
- Multi-threaded architecture exists but lacks AR functionality
- No camera integration implemented
- No AR-specific canvas rendering function
- Missing camera parsing logic from CHTML

Tasks needed to implement AR functionality:

1. CAMERA INTEGRATION
   - [ ] Add camera functionality to model.c (V4L2 camera interface)
   - [ ] Implement camera initialization, frame capture and processing
   - [ ] Add camera frame data structure to model.c
   - [ ] Create thread-safe camera data sharing mechanism

2. AR CANVAS RENDERING
   - [ ] Create canvas_render_camera function in 3.view.c (similar to the single-threaded version)
   - [ ] Implement 3D AR rendering with camera background
   - [ ] Ensure proper depth handling (using the issue fix from single-threaded version)
   - [ ] Add mutex protection for camera data access

3. CHTML PARSING FOR AR
   - [ ] Add 'ar' attribute parsing to canvas elements in 3.view.c
   - [ ] Modify canvas element creation to assign canvas_render_camera when ar="1"
   - [ ] Ensure backward compatibility with regular canvas elements

4. DEPTH HANDLING FIXES
   - [ ] Apply the 3D positioning fix from single-threaded version to multi-threaded
   - [ ] Use x, y, z coordinates from shapes properly instead of mapping 2D to 3D plane
   - [ ] Ensure proper 3D scaling with width, height, depth parameters

5. THREAD SAFETY
   - [ ] Add mutex protection for camera frame data
   - [ ] Ensure thread-safe access to shared shape data
   - [ ] Verify no race conditions in multi-threaded IPC system

6. CONTROLLER INTEGRATION
   - [ ] Add camera-specific event handling in 4.controller.c
   - [ ] Potentially add camera controls or AR-specific interactions

7. COMPILATION & LIBRARIES
   - [ ] Update xh.compile.dir.link.+x.sh to include camera libraries (-lavcodec, -lavformat, etc.)
   - [ ] Ensure make process includes necessary dependencies

8. TESTING
   - [ ] Test basic 2D/3D rendering still works
   - [ ] Test AR rendering with camera feed
   - [ ] Test multi-threaded performance remains stable
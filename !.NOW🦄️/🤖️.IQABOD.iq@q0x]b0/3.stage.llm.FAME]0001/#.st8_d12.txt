  Let me look at the forward_prop and backward_prop implementation to understand how they're
  connected and if the gradient flow is being preserved properly:

  The problem might be that I changed the architecture too dramatically. In a typical transformer
  training loop, you want to:
   - Process one token (or batch of tokens)
   - Compute forward pass
   - Compute loss
   - Compute gradients via backprop
   - Update weights
   - Repeat

  But with my current implementation, I'm:
   - Processing ALL tokens for attention/forward passes (epoch-wise)
   - Computing ALL losses
   - Running ALL backprop
   - Then updating weights

  This means that during backprop, it's not getting the correct immediate loss gradients from the
  corresponding forward pass. This can break the learning process!

  To fix this, I should revert to a more traditional training loop while still keeping the
  architectural improvements:


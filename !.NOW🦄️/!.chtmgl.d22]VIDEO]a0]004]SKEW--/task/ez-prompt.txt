 this is our framework : chtml
  we made it from scratch to render chtml gui in open gl.we use it to hold gui 
  seperately so we can prompt smaller contexts 
  and get more focused code outputs from llms.
  its an "mvc" architecture :

  todo: impliment Video Support (MP4):
     - <video src="path.mp4" x="..." y="..." width="..." height="..." autoplay loop 
  />
     - Use libav* from FFmpeg for decoding (avformat/avcodec/avutil).
     
     keep it single threaded
     
       2) Add frame queuing with timestamp-based rendering like in the reference
    3) Keep the audio synchronization using the same pattern

  use @task/0.mp4.playerffmpg]PURE!.c ; its proven to work on this computer. so do 
  everything it is doing 
  first before trying to get fancy . make this work at a basic level then we can 
  examine advanced features.
  
  bak/ backup any code before editing it. 

  Analyze the existing reference code for MP4 player with FFmpeg  │
	  Understand chtml framework architecture and MVC structure       │
 │    ○  Design video element parser for <video> tag                     │
 │    ○  Implement video controller with FFmpeg integration              │
 │    ○  Add video rendering using OpenGL textures                       │
 │    ○  Integrate frame queuing with timestamp-based rendering          │
 │    ○  Implement audio synchronization                                 │
 │    ○  Test video playback with sample MP4 files   

----------------------------------------------------------------------------
this is our framework : chtm (its inspired by html/js web design flow)
  we made it from scratch to render chtm gui in cli 
  (for standardized) cli apps/games .we use it to hold gui 
  seperately so we can prompt smaller contexts 
  and get more focused code outputs from llms.
  its an "mvc" architecture and modules in .c as plugins:
  
  
    to test, lets use our @chtm/index.chtm 
   navigation but add a 'canvas' 
   element to it , that we can navigate "p" player around using arrow keys
   
   we will reference the canvas in @reference/1.emoji_paint刷_cli🗡️]d7.c
   for a bit of inspiration (but not verbatim)
  
  the difference is that none of the paint (in canvas) code or 
  functionality should be hardcoded into the framework , 
  it should be within the "ipc" module system
  (so we may make a .c module to be executed)
  
  
  currently the canvas just needs to display designated size, 
  (ie : 
  
          
<!-- Canvas element -->
        <canvas id="game_canvas" width="8" height="8" label="Game Canvas" color="#CCCCCC"/>
    
  )
  *if we haven't added coloring elements, we should now, btw;
  and we will put a simple "p" that responds to arrow keys
  we will also add a text label element that communicates with ipc
  to display the players x & y position, get it? 
  
  
  
  
-------------------------------------------------------------------


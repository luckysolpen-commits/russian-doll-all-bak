You are an expert C/multimedia programmer adding image (JPG/PNG), video (MP4 with audio sync), and audio (MP3/OGG/WAV) support to CHTGL — using PulseAudio for playback and FFmpeg for decoding, NO GLEW.

Project Status:
- Rendering: raw OpenGL via glXGetProcAddress (immediate mode glBegin/glEnd for now).
- Textures load with stb_image.h + manual pointers (proven working).
- Goal: <img>, <video>, <audio> tags in CHTML + canvas texture from modules.
- Keep lightweight, RISC-V friendly.

Provided Files:
- view_v0.01.c (render loop, Shape rendering)
- model_v0.01.c (Shape/IPC)
- controller_v0.01.c (events)

Requirements:

1. Image Support (JPG/PNG):
   - <img src="path.jpg" x="100" y="100" width="200" height="200" />
   - Use stb_image.h (already working).
   - Cache textures by path.
   - UI overlay + canvas texture mode (SHAPE:type=texture;src="path.png";...)

2. Video Support (MP4):
   - <video src="path.mp4" x="..." y="..." width="..." height="..." autoplay loop />
   - Use libav* from FFmpeg for decoding (avformat/avcodec/avutil).
   - Decode video frames → upload as dynamic texture (glTexSubImage2D).
   - Decode audio frames → queue to PulseAudio.
   - Sync: use audio clock as master (track playback time, drop/delay video frames to match).
   - Play/pause/loop via attributes/events.

3. Audio Support (MP3/OGG/WAV):
   - <audio src="path.mp3" autoplay loop volume="0.8" />
   - Use PulseAudio simple API for playback.
   - Decode with FFmpeg avcodec if needed (direct for WAV, decode for MP3/OGG).
   - Multiple simultaneous sources.
   - Control: play/pause/volume/stop via module VAR/events.

4. Integration:
   - New UIElement types: "image", "video", "audio"
   - Texture quads with correct UVs (flip Y for images/videos).
   - Module can send SHAPE:type=texture;src="frame" for dynamic video canvas.

5. Performance:
   - Async decode thread for video/audio.
   - Texture caching.
   - Fallback placeholder.
   - Sync tolerance ~30ms.

Output:
- Patched view_v0.01.c with media rendering + PulseAudio/FFmpeg init.
- Necessary new code/files (pulseaudio playback func, ffmpeg decode loop).
- Example CHTML usage.
- Module example sending video texture.
- Link flags for -lavformat -lavcodec -lavutil -lpulse -lpulse-simple.

Prioritize sync accuracy and low latency.

Begin.
